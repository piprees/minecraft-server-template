# Platform default c2me config.
# recoverFromErrors regenerates chunks that hit errors during generation
# instead of leaving them in a broken state.
version = 3
globalExecutorParallelism = "default"

[chunkSystem]
	recoverFromErrors = true
