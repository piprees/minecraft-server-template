# scripts/seed/

The seed-rolling pipeline. Its architecture — every module, the tier system,
the warmup, the candidate store — is documented in the `seed-rolling` skill:

- `.claude/skills/seed-rolling/SKILL.md` — running it, reading its output, diagnosing it
- `.claude/skills/seed-rolling/references/pipeline-architecture.md` — how it is built
